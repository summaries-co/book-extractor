'''
TODO
- create a script that:
    - takes a pdf as input. pdf is formatted as {isbn}.pdf
    - creates a new folder labeled {isbn}_pages
    - splits the pdf into single page pdfs each labeled {isbn}_{page_number}.pdf

- make sure the code has
    - logs errors
    - pure functions (each function should only have one responsibility)
    - each function should have a helpful description
    - error handling if needed
'''